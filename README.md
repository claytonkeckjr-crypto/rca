# Relentless Calm Advisors — Landing Page (GoHighLevel embed)

Single self-contained block for Chris K. Jones / Relentless Calm Advisors LLC.
Built to paste directly into a **GoHighLevel HTML / Custom Code element** —
everything is inline (scoped `.rca` CSS, `@import` Google Fonts, inline SVG, one
`<script>`). No external CDN runtime, no build step.

**File:** `relentless-calm-advisors.html` — copy the whole thing into the element.

## What this round (v6) addressed — from the call w/ Chris (Jun 10)

- **Less is more / black-white-gold / militant feel** — hard edges, condensed
  type, intense. Not corporate, not soft.
- **Hero = the flyer.** Captures everything he does up top, pushes to one CTA.
- **Mission & Vision** block added under The Standard (interim copy — *swap for
  his official statement when it arrives*).
- **The Operation = one ecosystem, three fronts** (Executive / Resilient Voices /
  CEO Mindset) with a **Sphere of Influence "full circle"** diagram —
  everything feeds everything.
- **Frameworks** shown, not explained — pushes to the alignment call. Kept
  **Marksman + Spotter** (per the decision to keep it on the call).
- **Testimonials = the proof.** Featured + grid + auto-scrolling strip, each
  **tagged to the branch it fits** ("where it fits within the branding").
- **The Man** — B&W portrait; **war planning / "game of risk" with generals and
  nations / defensive-coordinator** strategy emphasis.
- **"Relentless Drive & Calm Mastery"** rendered as a heavier, masculine stamp.
- **Booking = questionnaire GATE → calendar.** Step 1 collects who/why and
  filters solicitors before any slot can be picked; Step 2 is the calendar.
  *(Demo calendar — wire to Chris's live Calendly + intake questions.)*
- **SEO** — keyword-rich semantic copy + JSON-LD structured data for future
  Google / Meta ads. (Meta `<title>`/`<meta>` tags live in GHL's page settings,
  not in this element.)

## Still needed from Chris (placeholders are in the file, marked with comments)

- Preferred **hero image** (the flyer / linen-shirt B&W). Drop the
  "looking-off-to-the-side" shot once supplied.
- Official **mission & vision statement** (interim copy is in place).
- New **testimonials doc** (with the branch each one fits).
- Final **logo** file / color variants.
- Live **Calendly link** + the pre-booking questionnaire (replaces the demo).

## Notes

- The page is fully visible/usable if JS is stripped (reveals, mobile menu, and
  the booking calendar all degrade gracefully).
- `prefers-reduced-motion` is respected (animations off, marquee becomes
  scrollable).
